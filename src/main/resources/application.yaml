spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa_hibernate_01
    username: postgres
    password: krya
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
